version: "3.8"

services:
  trilium:
    image: zadam/trilium:latest
    container_name: trilium
    ports:
      - "9988:8080"   # access via http://localhost:8080
    volumes:
      - /Users/zhangzhengzheng/Documents/trilium-data:/root/trilium-data   # persist notes and DB
    restart: unless-stopped